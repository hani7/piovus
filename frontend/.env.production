# URL de votre API Django en production (ex: https://api.votre-domaine.dz/api)
# Laissez vide ou commentez si le frontend et le backend partagent le même domaine (Vite proxy fallback n'est pas utilisé en prod, donc mettez toujours le chemin complet ou relatif)
VITE_API_URL=https://votre-domaine.dz/api
